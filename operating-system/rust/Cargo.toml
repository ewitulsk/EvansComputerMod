[package]
name = "terminal-os"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"  # Optimize for size
lto = true
panic = "abort"  # No unwinding

# Disable std library for smaller WASM output
[features]
default = []
